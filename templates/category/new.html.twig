{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add a new category</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type='submit' value='Ajouter une catégorie'>
    {{ form_end(form) }}
{% endblock %}
