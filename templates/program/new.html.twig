{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create a new program</h1>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <input type='submit' value='Ajouter une série'>
    {{ form_end(form) }}
{% endblock %}
